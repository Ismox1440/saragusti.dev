export const metadata = {
    title: "Ismael Saragusti - Sobre mi",
    description: "informacion sobre mi"
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
