export const metadata = {
    title: "Ismael Saragusti - Discord Status",
    description: "Mi estado actual en discord"
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}