export const metadata = {
    title: "Ismael Saragusti - Proyectos",
    description: "Mis proyectos"
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}